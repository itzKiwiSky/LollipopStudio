return {
    {
        name = "refresh",
        description = "refresh the module registry",
        run = function(name, ...)
            --% read the dir finding modules
            
        end,
    }
}